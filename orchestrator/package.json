{
  "name": "durable-coding-agent-orchestrator",
  "version": "1.0.0",
  "description": "Durable Function orchestrator for the coding agent",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agentcore": "^3.800.0",
    "@aws/durable-execution-sdk-js": "^1.1.6"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
